# Submission freeze — 2026-08-28 12:07 UTC

**Frozen at commit `09f4a9c` + the doc-truth pass below.** From here:
no new features. Only (a) `./finalize.sh` re-measurement after the owner re-authenticates,
(b) the video, (c) the Devpost/article/social publishing.

## Change control applied
- **MUST DO (done):** pre-action proof gate · jobs demo reproducing the BYOF incident ·
  reconnaissance before planning · over-blocking fix · cross-asset truth pass.
- **HIGH ROI (done):** hero-receipt renderer from real missions · adversarial eval harness ·
  architecture diagram 2.0 · eligibility matrix · authority model.
- **NICE TO HAVE (not built, deliberately):** Cloud TTS spoken receipt (probe returned 403, API
  not enabled; adds deploy surface days before the deadline for at most +0.2) · judge-facing HTML
  mission viewer (the receipt JSON + rendered hero image already carry the story).
- **DO NOT DO:** Veo/Lyria decoration · restructuring toward another track · any claim the
  running service cannot currently back.

## Verified at freeze
- 188 tests · lint clean · typecheck clean
- No key material in git history (4 scanner hits are the redactor's own fake fixture)
- No cross-asset contradictions: consensus overclaim removed, "no API key deployed" corrected to
  name the optional Secret Manager auditor key, test counts synced to 188
- One canonical URL everywhere; the stale duplicate project is referenced nowhere
- 16 live missions measured: 0 false-proven, 0 prohibited actions sent

## Known and stated, not hidden
- Gemma auditor reports `armed: false` until the owner enables Gemini-API billing; every asset
  now describes it as demote-only with an honestly-reported armed state, never as always-on
  consensus.
- The last eval predates the over-blocking and reconnaissance fixes; `finalize.sh` re-measures.
- The video does not exist yet. It is the single largest remaining risk to the score.
